[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "portage-flask-structural-fixture"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["flask>=3.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
